# Narmir Reborn — TODO

**Purpose:** Live source of truth for active work. Completed work lives in [ARCHIVAL.md](ARCHIVAL.md). Architecture status: [game/ARCHITECTURE.md](game/ARCHITECTURE.md).

**Last updated:** 2026-07-19 — cleaned up after the A1–A2 series (boot cleanup, full kingdom-route + admin-route splits) and the A3/A4/A5 partial work completed this session. See the 2026-07-19 entry at the top of [ARCHIVAL.md](ARCHIVAL.md) for what was done and why. This file now holds only what's still open.

**Mode: LOCAL ONLY.** Commits on local branches / local `main`. No PRs, no remote review loop.

**Verification rule:** Nothing is "done" until traced on the live runtime path. Docs alone do not count.

**Execution rule:** Surgical changes only — fix root causes, no bandaids, do not ignore warnings/errors. Test + document each item before moving to the next.

---

## Local acceptance baseline

Run before considering any item done:

```bash
npm run lint
npm run architecture:accept
npm run check:command-boundary
npm run validate:game-tables
npm test
```

Optional, recommended for anything touching routes:

```bash
npm run test:systems
npm run smoke:combat-v2
```

---

## Open work items

### Turn pipeline (§3 of the original assessment — see ARCHIVAL.md for A3-1/A3-3/A3-4)

| ID | Item | Status |
|----|------|--------|
| A3-2 | Rename or document `game/turn.js` (not the pipeline — only map WIP helpers; the name is misleading). | **DONE** (2026-07-19) — scope changed on investigation: the file wasn't just misleadingly named, it was **completely unreferenced** (`calcDiscoveryChance`/`processLocationMapsWip`, its only two functions, both had separate, actually-used implementations elsewhere — `game/lib/data-transformations.js` and `game/lib/expeditions.js` respectively). Deleted rather than renamed/documented, since documenting dead code just leaves it around to confuse the next reader. While comparing the duplicate implementations, found a **real, live P0-class bug** in the actual production copy (`game/lib/expeditions.js`, called from `game/engine.js`'s `processTurn`): `processLocationMapsWip` re-read `k.discovered_kingdoms` fresh on every loop iteration instead of accumulating, so whenever 2+ scribe location-maps completed in the same turn for the same kingdom, each iteration silently overwrote the previous one's discovery — only the last-processed map survived. A third, test-only copy in `game/location-maps.js` (used by `test/kingdom-discovery-paths.test.js`, never by production) had already fixed this independently but was never wired into the real code, meaning the existing test provided **zero regression coverage for production** — exactly the "test exercises a different implementation than production" pattern already flagged once this session (see ARCHIVAL.md's Forge entry). Fixed the real bug in `game/lib/expeditions.js`; repointed the test to import from the production module instead of the dead one; added a new regression case (`path 3b`) for the two-completions-in-one-call scenario, verified it fails against the pre-fix code and passes against the fix (not just written and trusted). Deleted both dead files (`game/turn.js`, `game/location-maps.js`); fixed a stale `CLAUDE.md` claim that the turn-regen timer lived in `game/turn.js` (it never did — the real timer is `lib/boot.js`'s `setInterval`); removed the dead `game/turn.js` entry from `test-systems-harness`'s module-surface check. Full acceptance baseline green (lint, architecture:accept, `npm test` 84/84 incl. the new regression case, `npm run test:systems` 82/82), plus a direct live `processTurn` call against the largest kingdom in the local DB confirming no regression. |
| A3-5 | Audit fire-and-forget DB writes in `processTurn` (happiness history record is `.catch()`-only, not awaited). | **DONE** (2026-07-19) — root cause: `processTurn` is a plain synchronous function (not `async`), so it structurally cannot `await` anything — every fire-and-forget call here is a deliberate workaround for that, not an oversight (confirmed: `checkFogDiscoveries`'s own file header already documents this rationale explicitly). Audited all 3: **`recordHappinessHistory`** — safe as-is, idempotent (`ON CONFLICT DO UPDATE`), has its own internal try/catch that never lets the promise reject, side-table only (graphing feature, not core state). **`checkFogDiscoveries`** — safe as-is, self-healing by design (re-scans every turn scouts are allocated, skips already-discovered kingdoms, so one failed call is caught by the next). **`revealRingHexes`** — genuine gap, fixed: it's gated on `ring_completed`, a one-time transition derived from the already-persisted `scout_progress` total, so a transient failure on the exact turn a ring completes means that ring's map hexes are **never revealed again** — no future turn re-triggers the same transition. Added `fireAndForgetWithRetry` (one retry before giving up) rather than restructuring the trigger to run every turn (would add real per-turn DB cost to the hot path for a rare edge case). Added `test/engine-fire-and-forget-retry.test.js` (3 cases: succeeds first try, fails-then-succeeds, fails-both-times-never-throws) since this exact retry logic had zero test coverage before. Full acceptance baseline green (lint, architecture:accept, `npm test` 85/85, `npm run test:systems` 82/82), plus a live `processTurn` call confirming no regression. |
| A3-6 | Document all 5 `type: 'turn'` call sites and why research double-runs a turn. | **DONE** (2026-07-19) — the "5x" count was a stale pre-A2-3 estimate; real count is **4 production call sites**: `routes/kingdom-research.js:139` (`/research`), `routes/kingdom-turn.js:308` (inside `runTurn()`, used by `kingdom-gameplay.js`'s `/smithy/forge-tools` and `/search` — **not** by the main `POST /turn` route itself, despite `runTurn`'s own comment calling itself "used by ALL routes that consume a turn"; that comment is stale too), and `routes/kingdom-turn.js:402`/`404` (the real `POST /turn` route's own separate inline implementation, profiled/non-profiled branches of the same logical call). "Research double-runs a turn" is **not a bug** — `/research`, `/smithy/forge-tools`, and `/search` are all deliberately-designed "turn-consuming instant actions": spend one stored turn to get its normal passive effects *plus* one specific bonus action atomically, rather than requiring two separate round-trips. Verified no double-charge: `turns_stored` is decremented exactly once (`routes/kingdom-research.js:142`); the `study-discipline` command's own engine implementation (`game/lib/building-research.js`) never touches `turns_stored`. **Investigating `study-discipline`'s real implementation surfaced a much bigger, unrelated finding**: `game/lib/building-research.js` is the one actually wired into `engine.js`, but there were **three more duplicate `studyDiscipline` definitions** across dead files, which led to auditing the entire `test-systems-harness` module-surface list for the same orphaned-legacy-file pattern already found once this session (`game/turn.js`, `game/location-maps.js`, A3-2). Found and deleted **8 more fully-dead files** — `game/actions.js` (641 lines, 9 duplicate functions), `game/research.js`, `game/construction.js`, `game/expeditions.js` (a broken 1-line fragment, not even a real module), `game/scoring.js`, `game/upgrades.js`, `game/mercenaries.js`, `game/forge.js`, `game/effects.js`, `game/trade-routes.js` — each verified via a comprehensive whole-repo require-pattern search (not the narrower per-directory pattern used initially, which produced **false negatives** for several genuinely-live files — `game/attunements.js`, `game/xp.js`, `game/covert.js`, `game/combat.js`, `game/combat-resolver.js` were all wrongly flagged as candidates by the narrow check and confirmed alive before touching anything). Every deleted file's function names were cross-checked against `engine.js`'s actual `require()` sources to confirm the real, live implementation lives in `game/lib/*.js` or `game/prestige/`/`game/evolution/` instead. Fixed two stale doc citations pointing at the wrong (deleted) file (`ADMIN_WISHLIST_PLAN.md`, `game/COMMAND_COVERAGE.md`); left one harmless stale entry alone (`scripts/migrate-pg-placeholders.js`'s skip-list references a deleted file, but it's a completed one-time historical migration script — a stale skip-list entry for a nonexistent file is a no-op, not a bug). Updated `test-systems-harness`'s module-surface list to drop all 12 now-deleted entries. Full acceptance baseline green (lint, architecture:accept, validate:game-tables, `npm test` 85/85, `npm run test:systems` 72/72), plus live `processTurn` and `calculateScore` calls against the largest kingdom in the local DB confirming no regression. |
| A3-7 | Align postfetch + `structureUpdates` field lists with client domains. | **DONE** (2026-07-19) — both halves had real, live gaps. **Postfetch side:** `routes/kingdom-turn.js`'s two postfetch sites (inside `runTurn()`, and the `POST /turn` route's own inline copy) refetch columns that `resolveExpeditions`/`resolveResourceHarvests` (game/engine.js) write via raw SQL outside the normal `updates` return chain — but the field list was stale. Audited every column reachable through that path (`game/lib/gameplay.js`'s `expeditionRewards`, `resolveExpeditions`'s own outer-loop writes, `resolveResourceHarvests`, and `game/lib/achievements.js`'s `checkAchievements` — reachable via `expeditionRewards` calling it by reference, a mutation-across-file-boundary the first grep-only pass missed) and found **15 real columns missing** from the refetch: `clerics, discovered_kingdoms, engineers, food, iron, items, level, population, res_spellbook, stone, wood, world_fragments, xp, xp_sources, war_machines`. Extracted the list into a shared `EXPEDITION_TOUCHED_COLS` constant (was duplicated inline in both places) and added the missing columns to both sites; also fixed a real asymmetry found in the same code — `runTurn()`'s postfetch (used by `/smithy/forge-tools` and `/search`) never called `convertNumericFields` on the refreshed row, unlike the main `/turn` route's copy, meaning numeric fields reached those two routes' responses as strings. This can never be a structural guarantee (resolveExpeditions's writes are fundamentally dynamic against `VALID_KINGDOM_COLS`, not a fixed list — restructuring it to return which columns it touched was considered and rejected as too invasive for this item's scope, given it's a frequently-exercised reward path); documented that honestly in a comment rather than claiming exhaustive completeness. **`structureUpdates` side (`routes/response-structurer.js`):** live-tested the expanded field list through `structureUpdates` against a real kingdom row and found two more real gaps, not previously caught: (1) `war_machines` matched **no domain Set at all** — it was silently dropped from every response instead of landing in `military.troops`, confirmed against `client/src/stores/militaryStore.js`'s `useWarMachines` hook, which reads exactly that path — a real, live bug (any war-machine count change reaching a client via the turn/updates flow was invisible until the next full reload) affecting normal `/warfare` war-machine purchases too, not just this postfetch change; fixed by adding it to `troopFields`. (2) `achievements` and `racial_bonuses_unlocked` are real columns that were logging as "unmapped" (A4-10's dev warning) even though nothing is actually broken — both legitimately have no Zustand-store consumer (`achievements` is fetched on demand by `LoreAndAchievements.jsx` via its own dedicated `GET /api/kingdom/lore-and-achievements`; `racial_bonuses_unlocked`'s only player-facing feedback is the system event message pushed when it unlocks, and its only UI surface at all is the admin `KingdomEditModal` raw field editor). Added an explicit `serverInternalOnlyFields` Set so these are intentionally, silently excluded instead of either falsely warning or (worse) papering over a real future gap by suppressing the warning mechanism entirely. New `test/response-structurer.test.js` (4 cases) covers all of this, including that the warning mechanism itself still fires correctly for a genuinely-new unmapped key. **Also found and fixed in passing:** `game/achievements.js` (176 lines) was a 13th orphaned pre-refactor duplicate from the same family as A3-2/A3-6's 12 deletions — `game/lib/achievements.js` is the only one actually required anywhere (confirmed via whole-repo require-pattern search, not the narrower per-directory one that produced false negatives earlier this session); deleted. Full acceptance baseline green (lint, architecture:accept, check:command-boundary 24 files, validate:game-tables, `npm test` 86/86, `npm run test:systems` 72/72), plus live verification: a real `runTurn()` call against a freshly-seeded kingdom (`test-systems-harness`'s seed helpers) confirming `war_machines` and all newly-added fields flow through correctly with no `structureUpdates` warnings, and a direct query against the largest kingdom in the local DB confirming the expanded `EXPEDITION_TOUCHED_COLS` SELECT runs cleanly with correct numeric/JSON typing after `convertNumericFields`. Dev server restarted twice (once per code change) with clean boot logs both times. |
| A3-8 | Incremental extract of attunement blocks / turn-systems registry (behavior-preserving). | **DONE** (2026-07-19) — the "turn-systems registry" half turned out to be dead scaffolding, not something to build on: `game/turn-systems.js`'s `SystemRegistry`/`TurnSystem` classes and `game/systems/gold-income.js` (a `GoldIncomeSystem` registered into it) existed but **`registry.processAll()` was never called anywhere in the codebase** — `goldIncomeSystem` was registered at module load and then permanently inert. The real, live gold-income logic is a separate inline block in `processTurn` (engine.js) that had since diverged significantly from the dead duplicate (trade income, a rebellion-respecting base, `gold_income` for the client's resource-rate display — none of which `GoldIncomeSystem` had). Reviving it would also have introduced a real bug: the registry's single merged-state-per-system interface would feed `goldPerTurn`/`calculateTradeIncome` the *post-happiness-update* state, whereas the current inline code deliberately uses the pre-turn snapshot (`k`, not `{...k,...updates}`) for those two calls specifically — forcing the abandoned framework to fit would have quietly changed which turn's happiness feeds gold income. Deleted `game/turn-systems.js` and `game/systems/gold-income.js` (confirmed dead via whole-repo require search beforehand) rather than force-fit new work onto abandoned, semantically-mismatched scaffolding — this is what "no path of least resistance" means here: reviving unused scaffolding just because it exists is itself a path-of-least-resistance shortcut. **The "attunement blocks" half was the real, valuable extraction target**: `processTurn` had 18 near-identical hand-written blocks (granary, vault, barracks, walls, guard tower, outpost, training, castle, mausoleum, library, mage tower, smithy, market, shrine, tavern, school, farm, housing attunements — ~107 lines), each `measureAttunement(name, () => fn({...k,...updates}, events))` followed by `Object.assign(updates, result)`. Unlike gold-income, these all already used consistent merged-state semantics with no snapshot-timing subtlety, making them a safe, mechanical, data-driven-loop extraction. Replaced all 18 with a single `BUILDING_ATTUNEMENT_PROCESSORS` array + `runBuildingAttunements(k, updates, events)` loop (same order preserved exactly, same per-call state merge, same in-place `updates`/`events` mutation). Verified behavior-preserving two ways: (1) a git-stash-based before/after comparison with `Math.random` fixed and a high-happiness fixture (to rule out `rebellionCheck`'s own randomness as a confound) produced **byte-identical** `gold`, `gold_income`, and full `updates` key set; (2) new `test/engine-building-attunements.test.js` (3 cases) locks in that the loop wires a real active attunement (granary / Tears of the World Tree) identically to calling the function directly, and that `updates`/`events` are mutated in place rather than replaced. Updated `test-systems-harness`'s module-surface list and the now-dead optional gold-income probe in suite 04 to match. Full acceptance baseline green (lint, architecture:accept, check:command-boundary 24 files, validate:game-tables, `npm test` 87/87, `npm run test:systems` 71/71 — one fewer than before since `game/turn-systems.js`'s module-surface entry was removed, not a regression). Dev server restarted, clean boot. |

### Client state path (§4 — see ARCHIVAL.md for A4-1/A4-2/A4-9/A4-10)

| ID | Item | Status |
|----|------|--------|
| A4-3 | Eliminate direct `receiveServerSnapshot` outside the normalizer (HirePanel, AuthModal, BuildPanel, ExplorationPanel, HeroesPanel, WarfarePanel, TrainingPanel, Forge sections, HappinessWidget, RankingsPanel, StatusPanel, VolcanicHexCard, useRegenCountdown, etc.). **Blocked on tracing each of the 12 raw-response sites (found in A4-1) against its actual client call site first** — some may be intentionally raw for a CommandHandler-bypass system (forge/evolution) consumed by a matching direct-snapshot call; wrapping in `structureUpdates` blind risks breaking currently-working client code. Do this per-route, not in bulk. | **IN PROGRESS** (2026-07-19) — traced per-route as required; found this is genuinely mixed, exactly as the item warned. **Ruled safe, no fix needed:** `ExplorationPanel.jsx`, `AuthModal.jsx`, `HeroesPanel.jsx` fan out the same flat full-kingdom snapshot to all 5 stores directly — verified every store's `receiveServerSnapshot` is written defensively (`if (data?.X !== undefined) state.X = data.X`), which is exactly the "full state snapshot (page load, turn start)" use case each store's own doc comment describes; wrapping these in `structureUpdates` would add code for no behavior change. **Found and fixed two real, confirmed-live bugs** (not hypothetical — verified via direct calls against real seeded DB data before/after): (1) `POST /kingdom/hire` (`routes/kingdom-gameplay.js`) sent its `updates` object **flat** (`{gold, population, fighters, troop_levels, updated_at}`), never passed through `structureUpdates`. `HirePanel.jsx`'s hire/fire handlers checked `res.updates.economy` etc., which was always `undefined` on a flat object — **hiring or firing units never updated any Zustand store through this path**, silently, since forever. Fixed by wrapping the route's response in `structureUpdates()` and replacing the client's 5 dead conditional checks with a single `normalizeAndRouteResponse()` call (verified byte-for-byte against real seeded-kingdom data: gold→economy, population→population, scribes→profile, troop_levels→military, all correctly routed, zero unmapped-key warnings). (2) `POST /kingdom/attack` (`routes/kingdom-warfare.js`) had the same flat-response bug, compounded by `WarfarePanel.jsx`'s client code checking field names that never existed in the real shape at all (`result.troops`, `result.morale` — the real field is `happiness`, not `morale` — and reading from the wrong nesting level entirely, `result.X` instead of `result.updates.X`) — **a real military attack has never updated troop counts, happiness, XP, level, gold, or turns_stored in the client stores**, only whatever a later, unrelated refresh happened to catch up. Also found and fixed a second gap in the same route while there: `turns_stored` (decremented via raw SQL) and `gold` (can be bumped by a bounty claim, also raw SQL) were never refreshed into the response at all, matching the exact "raw-SQL write outside the returned updates object" pattern from A3-7 — added a postfetch of `maps, gold, turns_stored` (`convertNumericFields`-corrected) before `structureUpdates()`. Verified end-to-end against real seeded combat data: fighters/rangers/etc. → `military.troops`, xp/level/turns_stored → `profile`, happiness → `population`, gold/maps → `economy`, zero unmapped warnings. Full acceptance baseline green (lint, architecture:accept, check:command-boundary, validate:game-tables, `npm test` 87/87, `npm run test:components` 172/172, `npm run test:systems` 71/71). **A third confirmed-live instance, fixed the same way**: `POST /kingdom/spell` (`routes/kingdom-warfare.js`) had the identical flat-response bug, and `WarfarePanel.jsx`'s `castWspell` didn't sync any store at all afterward (`SpellCastingModal.jsx` calls it fire-and-forget, return value discarded) — casting a spell never updated mana/scrolls/target-troops in the client stores. Fixed the route with `structureUpdates()` (+ `convertNumericFields`) and added a `normalizeAndRouteResponse()` call in `castWspell`. Also found `active_effects` (spell buffs/debuffs) has no Zustand-store consumer at all (only admin-tool visibility, `KingdomWidgets.jsx`/`KingdomEditModal.jsx`) — added it to `response-structurer.js`'s `serverInternalOnlyFields` alongside `achievements`/`racial_bonuses_unlocked`, same reasoning. Verified against real seeded spell-cast data (mana/scrolls/maps → economy, zero unmapped warnings). Full baseline green again after this change. **Still open, not yet traced**: `BuildPanel.jsx` (4 sites), `TrainingPanel.jsx` (2), the 4 Forge sections, `VolcanicHexCard.jsx`, `HappinessWidget.jsx`, `StatusPanel.jsx`, `RankingsPanel.jsx`, `useRegenCountdown.js`. **`/kingdom/covert`'s 5 sub-ops (spy/loot/assassinate/sabotage/raid) were NOT traced or fixed** — see new item A4-11, a bigger, separate finding uncovered while tracing this one. |
| A4-11 | `WarfarePanel.jsx`'s entire Covert Ops tab is non-functional: `doWcovert = () => {}` is a literal empty stub. The tab renders a target picker and 4 buttons (Spy/Loot/Assassinate/Sabotage) that call `doWcovert('spy')` etc., but nothing happens — no API call, no unit-count input, no per-op parameter (lootType/bldType) selector exists in the UI at all. The server side is fully live and tested (`test:systems`'s `covert` suite, 7/7 passing) via `POST /kingdom/covert`; only the client UI was never finished. This is a half-connected system (built server, unwired client), not a normalizer/wiring bug like A4-3's other findings — real feature-completion work, not a quick fix. | **DONE** (2026-07-19) — implemented the missing UI: a "units to send" quantity input (shared across all 4 ops, matching the server's single `units` param) plus a `lootType` dropdown (Loot), `unitType` dropdown (Assassinate), and `bldType` dropdown (Sabotage), each populated from the exact accepted-value lists in `game/covert.js` (`covertLoot`'s loot categories, `covertAssassinate`'s valid unit types, `covertSabotage`'s `BLD_MAP` keys) rather than guessed. Implemented `doWcovert(op)` to POST `/api/kingdom/covert` with the right body per op, then route the response through `normalizeAndRouteResponse()` (same pattern as the attack/spell fixes above). **Found the same flat-response bug a fourth time**: all 5 of `/covert`'s sub-op responses (spy/loot/assassinate/sabotage/raid) sent `updates` flat, never through `structureUpdates` — fixed all 5. **Found and fixed a fifth, different bug in the same route while building this**: the route's catch-all error handler checked `err.statusCode`, but `httpError()` (this file's own error constructor) sets `err.status` — the property name never matched, so every validation error thrown inside `/covert` (newbie protection, insufficient units, invalid target, etc.) fell through to a generic `500 "Covert operation failed - please try again"` instead of its real status code and message. Caught this by actually using the new feature in the browser (per CLAUDE.md's UI-testing requirement) — clicking Spy against a turn-167 kingdom (under the turn-400 newbie-protection gate) surfaced the generic 500 message instead of the real "You are under newbie protection..." text; checked the server log, found the property mismatch, fixed it, verified via direct `fetch()` calls in the browser console that the correct status code (400/404) and specific message now reach the client. Full browser walkthrough: logged into a live session, opened Warfare → Covert, selected a target, entered a unit count, exercised all 4 buttons' dropdowns, confirmed requests reach `/api/kingdom/covert` with correct bodies and correct error propagation. Full acceptance baseline green (lint, `npm test` 87/87, `npm run test:components` 172/172, architecture:accept, check:command-boundary, validate:game-tables, `npm run test:systems` 71/71) plus the live browser verification above — this repo's actual full turn-400 gameplay path (a real successful spy/loot/assassinate/sabotage) wasn't reachable in this session (would require aging a kingdom 233+ turns), so the *success* response path is verified via the direct seeded-DB `structureUpdates()` check (all 4 ops produce correctly-domain-routed, zero-unmapped-warning output against real command-handler results) rather than a full live HTTP round trip past turn 400. |
| A4-4 | Unify `client/src/utils/api.js` / `api.mjs` (dual modules, ~30 vs ~27 import sites). | **TODO** |
| A4-5 | `apiCallAndSync` when `updates` present in a response. | **TODO** |
| A4-6 | Socket action results → same client apply path as HTTP (may not currently share it). | **TODO** |
| A4-7 | Single auth/bootstrap hydrate (`/kingdom/me` fragmented across multiple AuthModal snapshots). | **TODO** |
| A4-8 | Scout progress full-reload bug (ties R-2). | **TODO** |

### Mutator boundary / CommandHandler (§5 — see ARCHIVAL.md for A5-1/A5-2/A5-3/A5-6/A5-7)

| ID | Item | Status |
|----|------|--------|
| A5-4 | Expand boundary tooling: scan **`game/sockets.js`** for the same forbidden `engine.*` mutators; fail the local gate. | **DONE** (2026-07-19) — generalized `check-command-boundary.js` to scan files outside `routes/` (new `STRICT_FILES_ABSOLUTE` list, `game/sockets.js` added). Also found and fixed a second, separate gap in the same pass: `engine.covertSpy`/`covertLoot`/`covertAssassinate` were never in the `FORBIDDEN` pattern list at all (only attack/spell were covered) — meaning the check would have missed covert-mutator violations in *any* scanned file, not just sockets.js. Sanity-tested the gate actually catches a real violation before trusting it (temporarily reintroduced a forbidden call into a scratch copy, confirmed `check:command-boundary` fails with the right file:line, restored, confirmed clean again). |
| A5-5 | Migrate socket attack/spell/covert to `commandHandler.handle` (parity with HTTP warfare). | **DONE** (2026-07-19) — **scope changed after investigation, user confirmed.** Comparing the socket handlers against the real HTTP `/attack`/`/spell`/`/covert` routes in `kingdom-warfare.js` found the socket versions weren't just architecturally non-compliant — their business logic had drifted far behind: no available-units validation, no newbie-protection (turn<400) gate, no fog-of-war/visibility gate, only 2 of 9 attack unit types, no hero XP, no bounty claiming, no watchtower/signal-tower notifications. Then confirmed via `grep` across `client/src` that the shipped client **never emits any of these socket events** (`socket-client.js` only emits `chat:request_online`/`chat:global`/`chat:alliance`) — the handlers were unreachable dead code, still technically exploitable by a raw socket.io client bypassing the web UI and skipping newbie protection + fog-of-war entirely. Presented the user three options (delete / bring to full parity / flag-and-defer); user chose delete. Removed `action:attack`/`action:spell`/`action:spy`/`action:loot`/`action:assassinate` (~340 lines) plus the now-dead helpers they alone used (`withTransaction`, `applyUpdates`, `insertNews`, `notifyUnread`) and now-dead imports (`engine`, `incrementUnread`, `unreadNewsCache`). Real combat/spell/covert already works correctly via HTTP + CommandHandler — untouched, still 100% covered by `test:systems`'s `combat`/`spells`/`covert` checks (all green). No client-visible behavior change (nothing used these). Full acceptance baseline green (lint zero warnings, architecture:accept, check:command-boundary 24 files, validate:game-tables, `npm test` 84/84, `npm run test:systems` 83/83). |
| A5-8 | Optionally extend boundary check to flag new kingdom POSTs that never call `handle` and never appear on an allowlist. Low priority, high noise. | **TODO** |

### Git hygiene (local)

| ID | Item | Status |
|----|------|--------|
| G-1 | Local branch prune — many local branches still show `[origin/X: gone]` (remote deleted, local ref not cleaned up). Confirmed still present as of 2026-07-19 (`git branch -vv`). Needs explicit user go-ahead before pruning (destructive). | **TODO** |
| G-2 | Worktree inventory; close finished lanes. `git worktree list` currently shows only the main worktree — likely already resolved, re-check before acting. | **TODO** |
| G-3 | Ignore agent noise (`logs/`, `terminals/`, smoke logs) in `.gitignore`. | **TODO** |
| G-4 | Reconcile local `main` ↔ `origin/main` — **only when the user chooses.** Currently 203 ahead / 8 behind. Not part of any assessment; not a PR. | **TODO** |

### Player-facing reliability

| ID | Item | Status |
|----|------|--------|
| R-1 | Production turn latency — **guardrail, not an active issue.** Historical 2026-07-08 report, not reproduced since; local `processTurn` compute measured and ruled out as a cause (see ARCHIVAL.md A3-3). If latency reports return, check the HTTP/transaction layer first, not turn logic. | **GUARDRAIL** |
| R-2 | Scout progress resets on full page reload (ties A4-8). | **TODO** |
| R-3 | Worldmap smoke test (load, markers, fog stride 48 client/server parity). | **TODO** |

### Explicit cuts (do not schedule)

| Item | Why |
|------|-----|
| Full event-bus / outbox rewrite | Wrong model for Narmir |
| Big-bang `engine.js` split | Multi-week; use incremental A3-8 instead |
| PR/CI gate redesign for this campaign | Local only |

---

## Notes

- **Do not implement** speculative work beyond what's listed above without the user sessioning it on first.
- **Do not** re-inflate `index.js` — new boot concerns go under `lib/`.
- `CLAUDE.md`'s PR workflow does **not** apply to this local-only campaign.
- Systems harness recovery tip if ever needed: `b04214e2` / `chore/test-systems-harness`.
